{{!-- '<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <link rel="stylesheet" href="/css/styles-jl.css">
</head>
<body>
    <div id="main">
        {{> menu}}
    </div>

    
    {{!-- <button id="show-change-password">Change Password</button> --}}

    <div class="popup">
        <div class="close-btn" id="close-change-password">&times;</div>
        <div class="form">
            <h2>Change Password</h2>
            {{#if error}}
                <p class="error">{{error}}</p>
            {{/if}}
            <form class="form-element" action="/changePassword" method="POST">
                <div class="form-element">
                    <label for="currentPassword">Current Password:</label>
                    <input type="password" id="currentPassword" name="currentPassword" required>
                </div>
                <div class="form-element">
                    <label for="newPassword">New Password:</label>
                    <input type="password" id="newPassword" name="newPassword" required>
                </div>
                <div class="form-element">
                    <label for="confirmnewPassword">Confirm New Password:</label>
                    <input type="password" id="confirmnewPassword" name="confirmnewPassword" required>
                </div>
                <div class="form-element">
                    <input type="submit" value="Confirm">
                </div>
            </form>
        </div>
    </div>
    
    {{!-- <script>
        const changePasswordModal = document.querySelector(".popup");
        const showChangePasswordButton = document.getElementById('show-change-password');
        const closeChangePasswordModalButton = document.getElementById('close-change-password');

        showChangePasswordButton.addEventListener("click", function () {
            if (!changePasswordModal.classList.contains("active")) {
                changePasswordModal.classList.add("active");
            }
        });

        closeChangePasswordModalButton.addEventListener("click", function () {
            if (changePasswordModal.classList.contains("active")) {
                changePasswordModal.classList.remove("active");
            }
        });
    </script> --}}
</body>
</html>
' --}}